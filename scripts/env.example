# Optional pre-seed: ./scripts/onboard.sh reads this file.
# If missing/empty, onboard.sh prompts for the key securely (no echo) and creates it.
# Keep this file secret - it is gitignored.
CEREBRAS_API_KEY=csk-your-key-here
